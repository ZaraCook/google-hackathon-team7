import { PageSkeleton } from '../components/common'

function TimelinePage() {
  return (
    <PageSkeleton
      title="Timeline"
      description="Loading event layers, scheduled items, and time-flow views."
    />
  )
}

export default TimelinePage
